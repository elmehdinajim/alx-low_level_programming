#include <stdio.h>

/**
 * main - Entry point
 *
 *
 * Return: Always 0 (Sucess)
 */

int main(void)
{
	char c;

	for (a = 'a' ; a <= 'z' ; a++)
	{
		putchar(a);

	}

	for (a = 'A' ; a <= 'Z' ; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
